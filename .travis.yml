language: android
android:
  components:
    - tools
    - platform-tools
    - build-tools-25.0.1
    - android-24
    - extra-google-m2repository
    - extra-android-m2repository
jdk: oraclejdk8

cache:
  directories:
    - $HOME/gradle/wrapper

before_install:
  - chmod +x gradlew
